# About

My custom nvim setup.

# Structure

Plugins set under lua directory, each plugin is it's separate lua file for maintainability.

# Setup TS projects
Run `npm init --yes && npm init @eslint/config@latest` to setup linting for TS/JS projects.
